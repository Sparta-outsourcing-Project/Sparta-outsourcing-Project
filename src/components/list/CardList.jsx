export default function CardList() {
  // const { isLoading, isError, data } = useQuery('datas', getMostPopularThumbnails);
  // console.log(data);

  // if (isLoading) {
  //   return <div>로딩중...</div>;
  // }
  // if (isError) {
  //   return <div>오류가 발생하였습니다. </div>;
  // }

  return <div>list</div>;
}
